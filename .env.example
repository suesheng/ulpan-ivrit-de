# Optional. Server-only — never expose to the browser.
# When set, Standortwunsch and Träger forms post to /api/inquiry.
# Without it, the browser opens the visitor's mail app instead.
# BREVO_API_KEY=
# BREVO_SENDER=info@bifode.org

# Phase 2+: Moodle (not used by the public site).
# MOODLE_URL=https://ulpan.bifode.org
# MOODLE_WSTOKEN=
